# Inkfrog「关于」界面接入方案

> 目标：把单色墨水屏风格的「关于」页（两层：固件信息 → 作者信息）加到 fontexp 设置里。
> 载体：LVGL 8.3，EPD_3IN52 240×415 单色屏，`lv_font_misans_16`（中文）。

---

## 0. 现状核对（已实测确认）

| 项 | 结论 | 依据 |
|---|---|---|
| 屏幕 | 240×415 单色墨水屏（1-bit 黑墨） | EPD_3IN52 文档 |
| 设置主页 | 由 `main.c` 的 `settings_btn_event_handler()` 构建，标题「设置」+ 返回 + 一整排 `settings_row_create()` 行按钮 | main.c:878-932 |
| 行按钮样式 | 白底、2px 黑边、radius 4；左 24px Montserrat 符号，右 16px MiSans 中文 | `settings_row_create()` main.c:832 |
| 子页打开模式 | `xxx_open(return_screen)`：`lv_obj_create(NULL)` → 建 UI → `lv_scr_load_anim(scr, OVER_LEFT, 0,0,false)`；返回回调里 `settings_screen_rebuild_main()` | settings_screen.c:152 / :216 |
| 版本号 | 应用层**无**版本宏；只有镜像 `image.cfg` 里的 `"version":"0.4"` | build log |
| 编译时间 | 应用层无；需用 C 宏 `__DATE__ " " __TIME__` 新建 | — |
| 字体覆盖 | `关于作者信息固件版本编译时间开发者开源免费…` 等字**全部存在**于 `lv_font_misans_16`，无需重新生成字体 | 脚本扫 unicode 表 |
| 构建 | `gcc/Makefile` 用 `$(wildcard $(dir)/*.[csS])` 收录源文件，新增 `.c` 自动编译，**不用改 Makefile** | gcc/Makefile:55 |

---

## 1. 总体结构

```
设置页 (main.c)
 └─ 行按钮「关于」  ── on click → settings_about_open(scr)
        │
        ├─ 屏① 固件信息（新屏幕，title「关于」）
        │     ├─ 固件版本   v0.4
        │     ├─ 编译时间   BUILD_TIME
        │     └─ 作者信息 ›   ── on click → 屏②
        └─ 屏② 作者信息（新屏幕，title「作者信息」）
              ├─ 头像（1-bit 抖动位图 LV_IMG）
              ├─ hrp / Inkfrog 固件作者
              ├─ Bilibili  hrp8888
              ├─ GitHub    hrp518
              └─ 简介一行
```

两层各是一个独立 `lv_obj_create(NULL)` 屏幕，遵循现有 `settings_*_open()` 模式；两层之间的返回：
- 屏② 返回 → 屏①（保存屏①指针，`lv_scr_load_anim` 回去）
- 屏① 返回 → 设置主页（`settings_screen_rebuild_main()`）

---

## 2. 资源准备：头像转 1-bit 抖动位图

真机只能显示黑/白，彩色照片必须做 Floyd–Steinberg 抖动转成 1-bit 点阵，再用 LVGL `LV_IMG_CF_INDEXED_1BIT` 图片载入（2 色 palette：黑/白）。

新增脚本 `tools/avatar_to_lvimg.py`（放 FontExp 项目根），运行一次生成 `avatar_eink_img.c`：

```python
# avatar_to_lvimg.py  (在 Windows/python 环境跑，产物贴进固件)
from PIL import Image

SRC  = r"C:\Users\hrp\Documents\彩色头像.png"
OUT  = "avatar_eink_img.c"
W = H = 96            # 屏②头像显示尺寸(设备像素)，可调
CF   = "LV_IMG_CF_INDEXED_1BIT"

im = Image.open(SRC).convert("RGBA")
bg = Image.new("RGBA", im.size, (255,255,255,255)); bg.alpha_composite(im)
g  = bg.convert("L").resize((W, H), Image.LANCZOS)
d  = g.convert("1", dither=Image.FLOYDSTEINBERG)   # 1-bit: 0=黑 1=白

# 转成每字节 8 像素, bit 顺序 LSB=左
rows = []
pix = d.load()
for y in range(H):
    byte = 0
    for x in range(W):
        if pix[x, y] == 0:      # 黑墨 → bit=1 (INDEXED_1BIT 调色板[1]=黑)
            byte |= (0x80 >> (x & 7))
        if (x & 7) == 7:
            rows.append(byte); byte = 0
    if (W & 7): rows.append(byte)

data_size = len(rows)
body = ",".join("0x%02X" % b for b in rows)
lines = body.split(",")
# 每行 12 字节排版
fmt = ",\n".join(",".join(chunk) for chunk in
      [lines[i:i+12] for i in range(0, len(lines), 12)])
open(OUT, "w").write(f"""/* 由彩色头像经 Floyd-Steinberg 抖动生成的 1-bit 墨水屏位图 (自动生成) */
#include "lvgl/lvgl.h"

LV_ATTRIBUTE_MEM_ALIGN
static const uint8_t avatar_bitmap[] = {{
{fmt}
}};

const lv_img_dsc_t avatar_eink_img = {{
    .header = {{ .w = {W}, .h = {H}, .cf = {CF} }},
    .data_size = {data_size},
    .data = avatar_bitmap,
}};
""")
print("written", OUT, data_size, "bytes")
```

> 说明：`INDEXED_1BIT` 在 LVGL 8.3 属于 `LV_IMG_CF_INDEXED_1BIT`（8.3 新命名，8.3 之前叫 `LV_IMG_CF_ALPHA_1BIT` 语义不同）。若该宏不存在可改用 `LV_IMG_CF_TRUE_COLOR` + 2×2 调色，但会大 8 倍。集成时以实际 `lv_conf.h`/`lvgl` 版本宏名为准。
> 产物 `avatar_eink_img.c` 会被 wildcard 自动编译。

---

## 3. 版本 + 编译时间宏

新增 `version.h`（或并入 `prj_config.h`）：

```c
#pragma once
#define INKFROG_VERSION_MAJOR  0
#define INKFROG_VERSION_MINOR  4
#define INKFROG_VERSION_STR    "0.4"
#define INKFROG_VERSION_LONG   "Inkfrog v0.4"
// 编译时间由编译器自动填，无需手改（保证“编译时间”与本次构建一致）
#define INKFROG_BUILD_DATE     __DATE__
#define INKFROG_BUILD_TIME     __TIME__
```

在 `about_screen.c` 里这样显示：
```c
static char s_ver[64], s_build[64];
snprintf(s_ver,   sizeof s_ver,   "v%s",  INKFROG_VERSION_STR);
snprintf(s_build, sizeof s_build, "%s %s", INKFROG_BUILD_DATE, INKFROG_BUILD_TIME);
```

---

## 4. 新增 about_screen.c / about_screen.h

仿照 `settings_screen.c` 的 `settings_font_select_open()` 模式。

### 4.1 对外接口 (about_screen.h)
```c
#ifndef ABOUT_SCREEN_H
#define ABOUT_SCREEN_H
#include "lvgl/lvgl.h"
/* 打开“关于”页（屏① 固件信息）。return_screen: 返回时回哪（一般传设置主页） */
void settings_about_open(lv_obj_t *return_screen);
#endif
```

### 4.2 about_screen.c 骨架
```c
#include "about_screen.h"
#include "settings_screen.h"   /* 用于 settings_screen_rebuild_main() */
#include "lvgl/lvgl.h"
#include "version.h"
#include <stdio.h>

extern void epd_mark_refresh_pending(void);

static lv_obj_t *g_about_fw_screen = NULL;   /* 屏① */
static lv_obj_t *g_return_screen = NULL;

/* ---------- 屏② 作者信息 ---------- */
static void author_back_cb(lv_event_t *e) {
    (void)e;
    if (g_return_screen) { /* 回到屏① */ }
    g_about_fw_screen = NULL;
    lv_obj_clean(NULL);
    // 实际实现直接切回屏①：保存屏①指针略写，见下
}

/* ---------- 屏① 固件信息 ---------- */
static void fw_back_cb(lv_event_t *e) {
    (void)e;
    g_about_fw_screen = NULL;
    settings_screen_rebuild_main();   /* 回设置主页 */
}

void settings_about_open(lv_obj_t *return_screen) {
    g_return_screen = return_screen;
    lv_obj_t *scr = lv_obj_create(NULL);
    lv_obj_set_style_bg_color(scr, lv_color_make(255,255,255), 0);

    /* 标题 */
    lv_obj_t *title = lv_label_create(scr);
    lv_label_set_text(title, "关 于");
    lv_obj_set_style_text_font(title, &lv_font_misans_16, 0);
    lv_obj_align(title, LV_ALIGN_TOP_MID, 0, 8);

    /* 返回按钮（复用设置页返回按钮样式/回调思路） */
    lv_obj_t *btn_back = lv_btn_create(scr);
    lv_obj_set_size(btn_back, 60, 30);
    lv_obj_align(btn_back, LV_ALIGN_TOP_LEFT, 10, 42);
    ui_apply_static_btn_style(btn_back, 2, 4);   // 见备注①
    lv_obj_add_event_cb(btn_back, fw_back_cb, LV_EVENT_CLICKED, NULL);
    lv_obj_t *bl = lv_label_create(btn_back);
    lv_label_set_text(bl, "返回");
    lv_obj_set_style_text_font(bl, &lv_font_misans_16, 0);
    lv_obj_center(bl);

    /* 品牌行 */
    lv_obj_t *banner = lv_label_create(scr);
    lv_label_set_text(banner, "Inkfrog  途蛙单词卡 · 第三方开源固件");
    lv_obj_set_style_text_font(banner, &lv_font_misans_16, 0);
    lv_obj_align(banner, LV_ALIGN_TOP_MID, 0, 92);

    /* 信息行：固件版本 */
    lv_obj_t *r_ver = lv_obj_create(scr);
    lv_obj_set_size(r_ver, 220, 44); lv_obj_set_pos(r_ver, 10, 150);
    lv_obj_set_style_border_width(r_ver, 2, 0);
    lv_obj_set_style_border_color(r_ver, lv_color_make(0,0,0), 0);
    lv_obj_clear_flag(r_ver, LV_OBJ_FLAG_SCROLLABLE);
    lv_obj_t *lv_ver = lv_label_create(r_ver);
    lv_label_set_text(lv_ver, "固件版本");
    lv_obj_set_style_text_font(lv_ver, &lv_font_misans_16, 0);
    lv_obj_align(lv_ver, LV_ALIGN_LEFT_MID, 12, 0);
    lv_obj_t *rv = lv_label_create(r_ver);
    lv_label_set_text(rv, s_ver);                 /* "v0.4" 反白 chip */
    lv_obj_set_style_text_font(rv, &lv_font_misans_16, 0);
    lv_obj_set_style_text_color(rv, lv_color_make(255,255,255), 0);
    lv_obj_set_style_bg_color(rv, lv_color_make(0,0,0), 0);
    lv_obj_align(rv, LV_ALIGN_RIGHT_MID, -12, 0);

    /* 信息行：编译时间（略，同 r_ver，右侧 s_build） */

    /* 入口行：作者信息 › */
    lv_obj_t *r_author = lv_btn_create(scr);
    lv_obj_set_size(r_author, 220, 44); lv_obj_set_pos(r_author, 10, 254);
    lv_obj_set_style_bg_color(r_author, lv_color_make(255,255,255), 0);
    lv_obj_set_style_border_width(r_author, 2, 0);
    lv_obj_set_style_border_color(r_author, lv_color_make(0,0,0), 0);
    lv_obj_add_event_cb(r_author, author_open_cb, LV_EVENT_CLICKED, NULL);
    lv_obj_t *lat = lv_label_create(r_author);
    lv_label_set_text(lat, "作者信息");
    lv_obj_set_style_text_font(lat, &lv_font_misans_16, 0);
    lv_obj_align(lat, LV_ALIGN_LEFT_MID, 12, 0);
    lv_obj_t *chev = lv_label_create(r_author);
    lv_label_set_text(chev, LV_SYMBOL_RIGHT);   /* › */
    lv_obj_set_style_text_font(chev, &lv_font_montserrat_16, 0);
    lv_obj_align(chev, LV_ALIGN_RIGHT_MID, -12, 0);

    lv_scr_load_anim(scr, LV_SCR_LOAD_ANIM_OVER_LEFT, 0, 0, false);
}
```

**屏② 作者信息**（`author_open_cb` 里 `lv_obj_create(NULL)` 建屏，放：
- 居中头像 `lv_img_create` + `lv_img_set_src(scr, &avatar_eink_img)`（圆形可加 border-radius 视 EPD 渲染；真机单色下直接显示抖动方块即可）
- `hrp` / `Inkfrog 固件作者` 两个 label
- 两个反白 chip（`B`/`G`）+ 账号 `hrp8888`/`hrp518`：小 `lv_btn` 或 `lv_label` 黑底白字
- 简介 label、返回按钮（回屏①）

> 备注① `ui_apply_static_btn_style()` 是 main.c 里的静态函数，跨文件不可见，需在本文件里复制一份相同实现（照抄 main.c:839-876 处 `settings_apply_static_btn_style` 逻辑），或把它移到公共头/改为非 static。

---

## 5. 在设置主页加「关于」入口 (main.c)

在 `settings_btn_event_handler()` 里 `SD 卡诊断` 行之后追加一行：

```c
/* 关于 */
settings_row_create(scr, 360, 50, LV_SYMBOL_INFO, "关于", settings_about_btn_cb);
```
并新增回调（放在 `settings_font_sel_btn_cb` 附近，样式同现有回调）：
```c
static void settings_about_btn_cb(lv_event_t *e) {
    lv_obj_t *scr = lv_scr_act();     // 设置主页
    settings_about_open(scr);
}
```

**布局注意**：当前 6 行已占满 Y=80→410（pitch 56）。再加「关于」需把行距压一点，改为 7 行 pitch≈47：
把各行 Y 改为 `78, 125, 172, 219, 266, 313, 360`，行高 50 不变（最后一行底 410，仍 <415）。

---

## 6. EPD 刷新与内存注意

- 进 About 与**返回**时，都在回调里先 `epd_pause_refresh()`、`lv_obj_clean()` + 建屏、后 `epd_resume_refresh()` + `epd_mark_refresh_pending()`，与现有设置页一致，避免刷出半成品。
- 头像位图 `avatar_eink_img.c` 走 `LV_ATTRIBUTE_MEM_ALIGN`；96×96×1bit≈1.2KB，可放 flash（`.data`，只读），不占 SRAM。
- 反白 chip 用 `lv_label` 直接黑底白字，最简单且最省内存，不用额外样式对象。

---

## 7. 落地步骤清单

1. 跑 `tools/avatar_to_lvimg.py` → 生成 `avatar_eink_img.c`（放 FontExp 根）。
2. 新建 `version.h`（版本 0.4 + `__DATE__ __TIME__`）。
3. 新建 `about_screen.h` / `about_screen.c`（两层屏幕，`settings_about_open()`）。
4. 在 `main.c` 设置页加「关于」行 + `settings_about_btn_cb`，并把 7 行 Y 压缩到 pitch≈47。
5. （若跨文件用到 `ui_apply_static_btn_style`）把它提为公共函数或在 about_screen.c 内复制一份。
6. 编译（`make`），确认 FontExp.a 包含 `about_screen.o`。

---

## 8. HTML 样板 → LVGL 映射速查

| HTML 原型元素 | LVGL 8.3 实现 |
|---|---|
| 顶栏「‹ 关于」+ 时间 | 标题 label + 返回 `lv_btn`（无 MV 时间可不做） |
| Inkfrog 标题/副标题 | `lv_label` + `lv_font_misans_16` |
| 黑框信息列表行 | `lv_obj` 白底、2px 黑边、`LV_OBJ_FLAG_SCROLLABLE` 清除 |
| 反白 `v0.4` / `B` `G` chip | `lv_label` 黑底白字（`bg_color=黑, text_color=白`） |
| `作者信息 ›` 可点行 | `lv_btn` 白底 2px 黑边 + 点击回调 |
| 抖动头像 | `lv_img` + `avatar_eink_img`（1-bit） |
| 半色调装饰点 | 可省略（真机可用 `lv_line`/点阵，优先级低） |
| 编译时间 | `INKFROG_BUILD_DATE " " INKFROG_BUILD_TIME` |
