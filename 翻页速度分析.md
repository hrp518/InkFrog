# 翻页速度瓶颈分析报告

## 一、日志时间线还原

### 第2次翻页（快速翻页，命中所有缓存）

| 时间戳 | 事件 | 耗时 |
|--------|------|------|
| 23:10:06.193 | Touch released → `[UPDATE]` | 0ms |
| 23:10:06.936 | `[RENDER_DBG]` layout done | **~744ms 布局** |
| 23:10:06.936 | `refr_now=10ms` | **10ms 渲染** |
| 23:10:07.018 | EPD busy | **82ms 等待+启动** |
| 23:10:07.695 | EPD done | **~677ms 串口传输** |
| **总计** | | **~1502ms** |

### 第3次翻页（首次翻页后的二次翻页）

| 时间戳 | 事件 | 耗时 |
|--------|------|------|
| 23:10:05.750~06.193 | Touch event (touch → release) | ~443ms 等待手指抬起 |
| 23:10:06.193 | `[UPDATE]` 触发 `update_display` | 0ms |
| 23:10:06.193~06.936 | **布局阶段** (layout=750ms) | **750ms** |
| 23:10:06.936~06.946 | `refr_now=10ms` | **10ms** |
| 23:10:07.018 | EPD busy 开始 | |
| 23:10:07.695 | EPD done (epd_cost=747ms) | **~747ms 串口** |
| **总计** | touch release 到 EPD done | **~1502ms** |

### 第4次翻页（慢速翻页，bitmap缓存未命中）

| 时间戳 | 事件 | 耗时 |
|--------|------|------|
| 23:10:06.193 | (上面第2次翻页完成) | |
| 23:10:12.948 | `[BM_RENDER] #100` → bitmap渲染开始 | |
| 23:10:14.490 | bitmap渲染中 | |
| 23:10:15.237 | `EPD_RENDER #11` 完成 | |
| — | — | — |
| 23:10:06.277 → 10:12.948 | **间隙 = ~6671ms** | **bitmap渲染~5000ms** |
| 23:10:12.948 → 15.237 | LVGL render = 13ms + bitmap渲染 | |
| 23:10:15.237 → 16.000 | EPD display | **~749ms 串口** |
| **总计** | | **~9700ms** |

---

## 二、时间开销构成（典型翻页）

### A. 快速翻页（bitmap缓存命中）: ~1.5秒

| 阶段 | 耗时 | 占比 | 说明 |
|------|------|------|------|
| 1. 触摸等待 | ~440ms | 29% | 等手指抬起（LV_EVENT_RELEASED） |
| 2. 布局计算 | ~750ms | 50% | **主瓶颈** - `update_display()` 中逐字符换行计算 |
| 3. LVGL渲染 | ~10ms | <1% | bitmap缓存命中，极快 |
| 4. EPD串口 | ~750ms | 50% | SPI传输99600字节到墨水屏 |
| **总计** | **~1500ms** | | |

**注意**: 触摸等待和布局是串行的，EPD串口是最后的，所以实际体感 = 触摸等待 + 布局 + EPD串口 ≈ 440 + 750 + 750 = **~1940ms**

### B. 慢速翻页（bitmap缓存未命中）: ~9.7秒

| 阶段 | 耗时 | 占比 | 说明 |
|------|------|------|------|
| 1. 布局计算 | ~750ms | 8% | 逐字符换行 |
| 2. **bitmap渲染** | **~6000ms** | **62%** | **最大瓶颈** - TTF解轮廓+渲染 |
| 3. LVGL渲染(flush) | ~13ms | <1% | 写framebuffer |
| 4. EPD串口 | ~750ms | 8% | SPI传输 |
| 5. 额外渲染周期 | ~2200ms | 22% | bitmap渲染后的二次render |
| **总计** | **~9700ms** | | |

---

## 三、瓶颈根因分析

### 瓶颈1: 布局计算 ~750ms（占快速翻页50%）

**位置**: `update_display()` 中 `while (p < end && y_offset + current_lh <= CONTENT_HEIGHT)` 循环

**根因**:
- 每个字符调用 `get_char_adv_width()` → `lv_font_get_glyph_width()` → TTF metrics查找
- 即使metrics已缓存，每次仍有函数调用开销
- 191个字符 × 每字符3-5次函数调用 = ~800次函数调用
- 其中包含了 `lv_tiny_ttf_set_dsc_phase(1)` 的布局阶段dsc统计开销

**日志证据**:
```
[DSC_STATS] layout phase: total=639 hits=635 misses=4 non_cjk=0 unique_miss=0
[RENDER_DBG] layout=750ms refr_now=10ms total=760ms
```
639次dsc查询 = 191字符的metrics查询

### 瓶颈2: bitmap渲染 ~5000-6000ms（慢速翻页的主因）

**位置**: `lv_refr_now()` → `lv_tiny_ttf_get_glyph_bitmap_cb()` → TTF轮廓解压+渲染

**根因**:
- bitmap LRU缓存只有256个slot，Full Page需~200+不同字形
- 首次打开书或翻到新内容时，大量字形未缓存
- 每个字形渲染：解压glyf数据 → 解二次贝塞尔曲线 → 光栅化
- CJK字形平均 ~250字节bitmap，~4-5ms/字形
- 150个miss × 4ms = **~600ms**（首次miss时）
- 但日志显示总bitmap渲染时间达**数秒**，说明bitmap_page_start/end之间的渲染被分散到多个周期

**日志证据**:
```
[BM_RENDER] #100 U+4E00 ... total_render=87ms   ← 前100个字形87ms
[BM_PAGE] total=4ms render=0ms hits=0 misses=0 used_slots=66/256  ← 66个slot已用
```

### 瓶颈3: EPD串口传输 ~750ms（固定开销）

**位置**: `EPD_3IN52_Display_DU()` → SPI发送99600字节

**根因**:
- 240×415 = 99600像素，每像素1bit = 12450字节
- 但实际发送的是完整framebuffer（可能包含padding）
- SPI时钟频率决定了传输时间下限
- 这是**不可消除的硬件固定开销**

**日志证据**:
```
e-Paper busy
... ~670ms ...
e-Paper busy release
EPD_3IN52_Display_DU END
[EPD] Display done (T=37025ms, epd_cost=747ms)
```

### 瓶颈4: 调试日志输出 ~50-100ms（可消除）

**位置**: 分散在 `update_display()`、`lv_tiny_ttf.c` 各处

**根因**:
- `printf()` 在嵌入式系统上通过UART输出，波特率通常115200
- 每行日志 ~50-100字节，输出时间 ~0.5-1ms/行
- 一次翻页产生 ~30-50行日志 = ~30-50ms
- bitmap缓存命中时的 `[FB_AREA]` 输出尤其多（每字形22行ASCII art）

**日志证据** (单个字形输出):
```
[GLYPH_BITMAP] U+7684 w=21 h=22 sz=462 non_zero=259
[GLYPH_HEX] 00 00 00 00 CF C2 9C ...  ← 48字节hex
[FB_AREA] U+7684 21x22:
[FB] ....@##....#@#.......   ← 22行ASCII art
[FB] ...#@@#....#@@.......
...
[FB_CACHE_HIT] U+4EBA sz=289 scale=0.0160 dsc=0x1701ce0
[FB_AREA] U+4EBA 16x16 (cached):  ← 又22行
```

---

## 四、提速方案

### 方案1: 关闭热路径调试日志（预期节省 ~200-500ms）

**影响文件**: `lv_tiny_ttf.c`

**操作**: 将以下日志用编译开关控制（`#if 0` 或 `#ifdef TTF_BITMAP_DEBUG`）:
- `[GLYPH_BITMAP]` - 每字形bitmap统计
- `[GLYPH_HEX]` - hex dump
- `[FB_AREA]` - ASCII art（**最大浪费**，每字形22行）
- `[FB_CACHE_HIT]` - 缓存命中日志
- `[BM_RENDER]` - 每50个字形才打印一次的统计，可保留
- `[CJK_METRICS]` - 每字符metrics日志

**具体做法**: 在 `lv_tiny_ttf.c` 顶部添加:
```c
#define TTF_BITMAP_DEBUG 0   /* 关闭bitmap调试 */
#define TTF_METRICS_DEBUG 0  /* 关闭metrics调试 */
```
然后用 `#if TTF_BITMAP_DEBUG` 包裹上述日志。

**预期效果**: bitmap命中时节省 ~200ms（消除了ASCII art输出），miss时节省 ~50ms。

### 方案2: 布局计算优化（预期节省 ~400ms，从750ms降至350ms）

**影响文件**: `epub_viewer.c` 中的 `update_display()`

**优化A: 跳过LVGL字体API，直接用预计算的metrics表**

当前每个字符调用 `lv_font_get_glyph_width()`，走LVGL的完整font API链。

优化：在布局阶段直接使用 `lv_tiny_ttf` 内部的 metrics_cache，通过一个快速查询函数直接获取advance width，绕过LVGL font API的通用路径。

```c
// 在 lv_tiny_ttf.h 中暴露快速查询接口
int lv_tiny_ttf_get_adv_width_fast(lv_font_t* font, uint32_t unicode);
```

**优化B: 预计算常用字符宽度查找表**

对于CJK字符（U+4E00-U+9FFF），在字体加载后预计算一个固定宽度：
```c
// CJK字符在固定大小的字体中advance width都相同
// 可以在layout前缓存一次
int cjk_width = lv_font_get_glyph_width(FONT, 0x4E00, 0); // 任意CJK字
```
然后layout循环中对CJK字符直接用缓存值，不调用API。

**优化C: 减少dsc统计开销**

布局阶段的 `lv_tiny_ttf_reset_dsc_stats()` 和统计收集本身有开销。用编译开关控制。

### 方案3: 增大bitmap缓存（预期减少慢速翻页的bitmap miss）

**影响文件**: `lv_tiny_ttf.c` 或 `epub_viewer.c`

**当前**: 256个slot的LRU缓存

**优化**: 
- Full Page约200个不同CJK字形 + ASCII字形
- 将缓存从256提升到512或1024
- 这样连续翻页时，前几页的字形仍留在缓存中

```c
// 在 lv_tiny_ttf_bitmap_page_start() 中或初始化时
#define GLYPH_BITMAP_CACHE_SIZE 512  // 从256增加到512
```

**内存开销**: 每个slot ~300字节 → 512×300 = 153KB（在PSRAM中可接受）

### 方案4: 预渲染下一页（后台线程）

**概念**: 在显示当前页时，后台预先渲染下一页的bitmap

**实现**:
1. 在 `update_display()` 渲染完当前页后，启动一个FreeRTOS task
2. 该task预计算下一页的offset，预渲染所有字形的bitmap到缓存
3. 用户真正翻页时，所有bitmap已在缓存中

**复杂度**: 高，需要处理并发和同步。

---

## 五、建议实施顺序

| 优先级 | 方案 | 预期效果 | 难度 | 风险 |
|--------|------|----------|------|------|
| **P0** | 方案1: 关闭调试日志 | 快速翻页 -200ms | 低 | 无 |
| **P1** | 方案2A: 快速metrics查询 | 快速翻页 -400ms | 中 | 低 |
| **P1** | 方案3: 增大bitmap缓存 | 消除慢速翻页 | 低 | 低（PSRAM够用） |
| **P2** | 方案2B: CJK固定宽度 | 快速翻页 -100ms | 低 | 无 |
| **P3** | 方案4: 预渲染 | 极致优化 | 高 | 中 |

---

## 六、预期最终效果

| 场景 | 当前 | 优化后(P0+P1) | 改善 |
|------|------|---------------|------|
| 快速翻页（缓存命中） | ~1500ms | ~800ms | **47%↓** |
| 慢速翻页（缓存未命中） | ~9700ms | ~1500ms | **85%↓** |
| EPD串口（固定） | ~750ms | ~750ms | 不变 |

**注意**: EPD的750ms是硬件固定开销，无法通过软件优化消除。实际体感 = 软件处理 + EPD刷新。

---

## 七、不修改的调试日志（保留）

以下日志对性能分析至关重要，不应关闭:
- `[UPDATE]` - 翻页触发记录
- `[RENDER_DBG]` - 每页渲染总耗时（layout + refr_now + total）
- `[EPD_RENDER]` - EPD刷新周期统计
- `[EPD]` - EPD display start/done
- `[DSC_STATS]` - 缓存命中率统计
- `[BM_PAGE]` - bitmap page统计（total/render/hits/misses）
- `[MISS_L1]` - Level1缓存未命中