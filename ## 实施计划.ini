## 实施计划

我已经读完了 main.c（1-1000行），结合之前的 subagent 分析结果，整理出以下计划：

### 要修改的文件
1. **`main.c`** — 主要修改
2. **`screensaver.c`** — 添加无图片保护

### 具体步骤

#### 1. 删除 GPIO 扫描测试
- 删除 `gpio_scan_test()` 函数（约 line 1045）
- 删除 `main()` 中对它的调用

#### 2. 添加 ADC VBAT 读取 + 右上角显示
- 添加 `#include "driver/chip/hal_adc.h"`
- 初始化 ADC（`HAL_ADC_Init`）
- 定期调用 `HAL_ADC_Conv_Polling(ADC_CHANNEL_8, ...)` 读取 VBAT 电压
- 在 `main_ui_create()` 右上角添加 label 显示电压值（如 "3.70V"）
- 配置 PA21 为 GPIO 输入，检测充电状态（PA21=1 → 显示 "⚡"）

#### 3. PA6 按键触发屏保
- 配置 PA6 为 GPIO 输入 + 上拉
- 在 disp_task 中轮询检测 PA6=0（按下）
- 触发时：进入屏保 → EPD 深度睡眠

#### 4. 屏保无图片保护
- 检查 `screensaver.c` 中加载图片的逻辑
- 如果无图片：显示 "Tuwa Reader" 文字 → EPD 深度睡眠

