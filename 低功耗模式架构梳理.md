# 低功耗模式架构梳理

## 一、三种低功耗模式对比

| | **屏保/休眠** | **时钟模式** | **充电模式** |
|---|---|---|---|
| **触发** | PA6 按键 / 空闲 120s 超时 | 用户从首页 Sleep Clock 进入 | 休眠时插入充电器 / 普通模式插电时按 PA6 / 插电时空闲超时 |
| **MCU 状态** | HIBERNATION（最低功耗） | HIBERNATION（每分钟冷启动刷新） | **MCU 活跃**（不休眠，轮询 GPIO） |
| **唤醒方式** | 冷启动（HIBERNATION 唤醒 = 重启） | WKTIMER 定时 + PA6 按键 | 不需要唤醒（MCU 一直在跑） |
| **EPD 刷新** | 进休眠前刷 1 次（屏保画面） | 每分钟冷启动刷 1 次（时钟数字） | **只刷 1 次**（充电画面），之后静止 |
| **WiFi/SD/LVGL** | 关闭 | 冷启动不启动 | 关闭（进充电模式时）/ 不启动（PA21 早判路径） |
| **初始化路径** | 从 disp_task 进入，外设已运行，需关闭 | main() 早判，只 level0 + EPD | 两种：① PA21 早判（level0 + EPD）② disp_task 进入（外设已运行，需关闭） |
| **退出** | PA6 唤醒 → 冷启动 → 正常启动 | PA6 唤醒 → 冷启动 → 清标志 → 正常启动 | PA6 按下 → 正常启动 / 拔充电器 → 进 HIBERNATION |

---

## 二、冷启动分流（main() 入口）

```
MCU 冷启动 → main()
  │
  ├─ 读 HAL_Wakeup_GetEvent() (SystemInit→pm_init 已填充)
  │
  ├─ WKTIMER 唤醒?
  │    → level0 + EPD_GPIO → clock_minute_cycle()
  │      (画时钟 → 刷 EPD → 配 WKTIMER+PA6 → HIBERNATION, 不返回)
  │
  ├─ PA21(WKIO7) 唤醒 + PA21=0(充电中)?
  │    → level0 + EPD_GPIO → charge_mode_run_minimal()
  │      (画充电画面 → 刷 EPD 一次 → EPD_Sleep → 轮询 PA21/PA6)
  │        ├─ 拔充电器 → HIBERNATION (不返回)
  │        └─ PA6 按下 → return → 继续下面的正常启动
  │
  └─ 其它（上电 / PA6 从休眠唤醒）
       → level0 + boot_screen → level1(WiFi) + level2(SD) → LVGL → 首页
```

**关键点**：
- `HAL_Wakeup_GetEvent()` 在 `main()` 第一行就可读（SystemInit→pm_init 已填充）
- PA21 早判走**最小化路径**（level0），不启动 WiFi/SD/LVGL
- PA21 早判里 PA6 按下 → return 后继续正常启动（WiFi/SD/LVGL 全初始化）

---

## 三、各模式的 EPD 刷新策略

### 屏保/休眠模式
- **刷几次**：1 次（屏保画面）
- **何时刷**：关 WiFi 之前（用户立即看到画面）
- **刷什么**：屏保图片 或 "Tuwa Reader" 文字
- **刷新后**：EPD_Sleep，进 HIBERNATION
- **用 GC 还是 DU**：用 `EPD_3IN52_Init + EPD_3IN52_Display`（GC 全刷，清屏保画面的残影）

### 时钟模式
- **刷几次**：每分钟冷启动刷 1 次
- **何时刷**：clock_minute_cycle 里
- **刷什么**：大号时钟数字 "HH:MM"
- **刷新后**：EPD_Sleep，配 WKTIMER + PA6，进 HIBERNATION
- **用 GC 还是 DU**：整点用 GC（清残影），其余分钟用 DU（快）

### 充电模式
- **刷几次**：**只刷 1 次**（进充电模式时）
- **何时刷**：charge_render_and_refresh 里
- **刷什么**："Tuwa Reader"（EPD_DrawStringCentered 只支持这些字母）
- **刷新后**：EPD_Sleep，MCU 保持活跃轮询，不再碰 EPD
- **用 GC**：`EPD_3IN52_Init + EPD_3IN52_Display`（GC 全刷一次）

---

## 四、各模式的唤醒源配置

### 屏保/休眠（进 HIBERNATION 前）
```
PA6(WKIO2)  下降沿  → 唤醒进正常模式
PA21(WKIO7) 下降沿  → 唤醒进充电模式（插入充电器）
```
**为什么 PA21 用下降沿**：休眠时未充电，PA21=高。插入充电器拉低 → 下降沿。
**不会循环**：因为休眠时 PA21 是高，只有真正插入才会产生下降沿。

### 时钟模式（进 HIBERNATION 前）
```
WKTIMER     60s 定时 → 唤醒刷新时钟
PA6(WKIO2)  下降沿  → 唤醒退出时钟模式
```
**不用 PA21**：时钟模式和充电无关。

### 充电模式（MCU 活跃，不进 HIBERNATION）
```
不需要唤醒源！MCU 一直跑，每 200ms 轮询：
  PA21 == 高(拔出) → 进 HIBERNATION（配 PA6+PA21 下降沿唤醒源）
  PA6 == 低(按下) → return 进正常模式
```

---

## 五、各模式的外设初始化/关闭

### 屏保/休眠（从 disp_task 进入，外设已运行）
**关闭顺序**：
1. `loading_show("Going to sleep...")` → 推屏
2. `epd_take_ownership()` → 挂起 LVGL
3. 画屏保画面 → `EPD_3IN52_Display` → `EPD_Sleep`
4. `wifi_controller_stop()` → 停 wc_task
5. `pm_unregister_wlan_power_onoff()` → 屏蔽 SDK net_sys_onoff
6. `wifi_controller_poweroff()` → wlan_sta_disable + 等 Sys3 掉电
7. `CHSC6540_DeInit()` → 关触摸 I2C
8. `HAL_ADC_DeInit()` → 关 ADC
9. SD: `f_mount(NULL)` → `mmc_card_deinit` → `HAL_SDC_Deinit`
10. 配 PA6+PA21 唤醒源 → `pm_enter_mode(HIBERNATION)`
**不关 EXT LDO**（PA6/PA21 输入检测依赖 VDDIO）

### 时钟模式（从 disp_task 进入）
**关闭顺序**：同屏保（复用 `clock_shutdown_peripherals()`）
**进入顺序**：
1. NTP 校时 → 写 RTC（weekday=7 magic）
2. 画时钟 → 刷 EPD → EPD_Sleep
3. 关外设
4. 配 WKTIMER + PA6 → HIBERNATION

### 充电模式 — PA21 早判路径（冷启动，外设未启动）
**不需要关闭外设**（根本没启动）：
1. level0 + EPD_GPIO
2. 画充电画面 → 刷 EPD → EPD_Sleep
3. 轮询 PA21/PA6（MCU 活跃）

### 充电模式 — disp_task 进入（外设已运行）
**关闭顺序**：
1. `wifi_controller_stop()` → `pm_unregister` → `wifi_controller_poweroff()`
2. `CHSC6540_DeInit()` → `HAL_ADC_DeInit()`
3. SD: `f_mount(NULL)` → `mmc_card_deinit` → `HAL_SDC_Deinit`
4. 画充电画面 → 刷 EPD → EPD_Sleep
5. 轮询 PA21/PA6

---

## 六、RTC magic 标志

RTC weekday 字段（3 bit，正常值 0-6）用来存模式标志：
- **weekday=7** → 时钟模式（clock_mode）
- **weekday=6** → 充电模式（charge_mode）
- 正常值 0-6 → 普通模式

**注意**：NTP 校时只会在普通模式联网时做，会覆盖 weekday 为真实星期值。
充电模式/时钟模式不联网，不会互相覆盖。

---

## 七、僵尸模式日志分析（插电后快速按 PA6 卡死）

### 问题根因
日志行 335-490：
```
[PA6] Button pressed
, charger connected -> charge mode    ← disp_task 里 PA6+充电 → charge_mode_enter()
[CHARGE] entering charge mode
[WIFIC] Stopping controller...         ← 开始关 WiFi
... WiFi 关闭 ~2s ...
[CHARGE] render charge screen          ← 画充电画面
[EPD_Init] ... GC 刷新 ~2s ...
[CHARGE] EPD refreshed (once)
[CHARGE] entering poll loop (awake)    ← 进轮询
[LVGL_DIAG] loop=3000 ... gap=1000ms   ← LVGL 还在跑！
...
[LVGL_DIAG] loop=14600                 ← LVGL 跑了 14 秒
```

**卡死原因**：`charge_mode_enter` 从 `disp_task` 调用，关了 WiFi/SD/触摸，但：
1. **LVGL 线程（lvgl_task）还在跑**——没被挂起/删除
2. **disp_task 也还在跑**——charge_poll_loop 在 disp_task 里死循环
3. **font_warm timer 还在跑**——5s 后触发了 L1 preload（日志行 385-434）
4. **WiFi 没真正关掉**——`Sys3 STILL ALIVE (waited 3000ms)`（日志行 405）
5. LVGL 在 charge_poll_loop 期间不断尝试刷新 EPD（已被 Sleep），后台 timer 和 font_warm 互相干扰

### 正确修法
`charge_mode_enter`（从 disp_task 进入时）应该：
1. **先停 font_warm timer**（避免预热在充电模式触发）
2. **挂起 lvgl_task**（用 `vTaskSuspend(lvgl_thread.handle)`，和 `epd_take_ownership` 一样）
3. 关 WiFi/SD/触摸/ADC
4. 画充电画面 → 刷 EPD → EPD_Sleep
5. **恢复 lvgl_task**（让 disp_task 的 charge_poll_loop 和 lvgl_task 共存？不对——应该挂起 lvgl_task 直到退出充电模式）

或者更简单：**charge_mode_enter 不在 disp_task 轮询，而是像 charge_mode_run_minimal 一样画完画面后直接进 HIBERNATION**（配 PA6+PA21 唤醒源），唤醒后再冷启动分流。这样不需要 MCU 活跃轮询。

---

## 八、待确认的设计决策

1. **充电模式到底要不要 MCU 活跃？**
   - 用户说"一直保持唤醒" → MCU 活跃轮询
   - 但 MCU 活跃 + lvgl_task 还在跑 = 资源冲突
   - 方案A：挂起 lvgl_task + disp_task 轮询（复杂但符合用户要求）
   - 方案B：画完充电画面后进 HIBERNATION，配 PA6+PA21 唤醒（简单，唤醒=冷启动分流）

2. **充电画面显示什么？**
   - EPD_DrawStringCentered 只支持 "Hello,World!Tuwa Reader" 的字母
   - 需要加 'C' 'h' 'g' 字母才能显示 "Charge"
   - 或者直接加 'C' 一个字母支持即可
